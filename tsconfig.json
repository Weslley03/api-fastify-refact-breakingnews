{
  "compilerOptions": {
    "outDir": "./dist",   // Define a pasta de saída para os arquivos compilados
    "rootDir": "./",   // Define a pasta raiz dos arquivos TypeScript
    "target": "es6",      // Define a versão do JavaScript para a qual o código será compilado
    "module": "commonjs", // Define o sistema de módulos (use 'commonjs' ou 'esnext')
    "strict": true,       // Habilita verificações de tipo mais rigorosas
    "esModuleInterop": true,  // Facilita a importação de módulos CommonJS
    "skipLibCheck": true,     // Pula a verificação de tipos em arquivos de declaração
    "forceConsistentCasingInFileNames": true // Garante que o uso de maiúsculas e minúsculas nos nomes de arquivos seja consistente
  },
  "include": [
    "*.ts",
    "src/**/*.ts"  // Inclui todos os arquivos .ts na pasta src
  ],
  "exclude": [
    "node_modules"  // Exclui a pasta node_modules
  ]
}